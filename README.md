# hw07

```bash

npm i; 
npm run build;
# in another console:
npm run start;
# note that you can send data from wscat,
# and the browser refuses to parse the incomming data.
# I'm really not sure why this is happening.

```